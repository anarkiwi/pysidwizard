name: tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: lint (ruff + black)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dev dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: ruff check
        run: python -m ruff check src tests

      - name: black --check
        run: python -m black --check src tests

  test:
    name: pytest (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v7

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package and test dependencies
        # ``[dev]`` pulls in pytest + ruff + black + zstandard.
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Cache SID-Wizard 1.94 tarball + extracted SWMs
        # The tarball is ~9 MB; caching avoids hitting CSDB on every test
        # run. tests/_swm_cache.py downloads + SHA-256-verifies it directly
        # into tests/.swmcache; key on that module so a pin change busts it.
        uses: actions/cache@v4
        with:
          path: tests/.swmcache
          key: swm-1.94-${{ hashFiles('tests/_swm_cache.py') }}

      - name: Cache HVSC corpus tunes
        # HVSC tunes are copyright and never committed; the corpus tests fetch
        # them from the mirror into a gitignored cache. Cache that dir across
        # runs, keyed on the corpus path list + the fetch helper.
        uses: actions/cache@v4
        with:
          path: tests/.tunecache
          key: hvsc-tunes-${{ hashFiles('tests/_hvsc_corpus.py', 'tests/_hvsc_fetch.py') }}

      - name: Run tests with coverage
        run: |
          python -m pytest --cov=pysidwizard --cov-report=term-missing --cov-report=xml

      - name: Upload coverage artifact
        if: matrix.python-version == '3.12' && matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-xml
          path: coverage.xml
          if-no-files-found: warn
